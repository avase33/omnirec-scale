global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: omnirec-serving
    metrics_path: /metrics
    static_configs:
      - targets: ["serving-api:8000"]

  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
